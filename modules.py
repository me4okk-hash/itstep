import random as r
import string as s

# Задание 1

# name = input("Введiть iм'я: ")

# name1 = name + str(r.randint(100, 9999))

# separate = r.choice(["_", ".", "-"])
# letters = "".join(r.choices(s.ascii_lowercase, k=3))
# name2 = name + separate + letters

# prefixes = ["Pro", "Ultra", "Super", "Mega"]
# prefix = r.choice(prefixes)

# name_cap = name.capitalize()

# digits = "".join(r.choices(s.digits, k=2))

# combined = list(name_cap + digits)
# r.shuffle(combined)
# shuffle_name = "".join(combined)
# name3 = prefix + shuffle_name

# print(name1, name2, name3)

